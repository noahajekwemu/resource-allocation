# Resource Allocation System

## Credentials

Place the Google service account key file at:

```text
credentials/service_account.json
```

Do not commit credentials to git. The `credentials/` directory and JSON credential files are ignored by `.gitignore`.
